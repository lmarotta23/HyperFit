# HyperFit
Personal iPhone PWA to track weights lifted across a rotating set of workouts. Pick today's workout, log weights against fixed baselines, and get feedback on whether you're getting stronger, steady, or below baseline. Includes a read-only baseline reference and a calendar to browse past sessions. No backend — data stored locally via localStorage.
